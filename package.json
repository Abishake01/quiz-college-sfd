{
  "name": "doodle-quiz",
  "version": "1.0.0",
  "private": true,
  "description": "Sketchbook-style multiplayer MCQ quiz with admin dashboard",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "node --watch --env-file-if-exists=.env server.js"
  },
  "engines": {
    "node": ">=22.9"
  },
  "dependencies": {
    "pg": "^8.23.0"
  }
}
